# From algorithm 3.1
# Bracketing is a type of algorithm that tries to guess the range in which a minimum
# or several minimum's can fall.
# We end up with 3 points, the range [a,b] and a point c that is f(a) < f(c) < f(b)

